{% extends 'base.html.twig' %}

{% block title %}Editer Location{% endblock %}

{% block body %}
    <div class="container">
    <h1>Editer une Location</h1>

    {{ include('rent/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('rent_index') }}">retour à la liste</a>

    {{ include('rent/_delete_form.html.twig') }}
    </div>
{% endblock %}