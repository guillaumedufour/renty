{% extends 'base.html.twig' %}

{% block title %}Editer {{ rent.rentTitle }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editer {{ rent.rentTitle }}</h1>

        {{ include('rent/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('rent_index') }}">retour à la liste</a>

        {{ include('rent/_delete_form.html.twig') }}
    </div>
{% endblock %}