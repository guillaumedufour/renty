{% extends 'base.html.twig' %}

{% block title %}SectorArea{% endblock %}

{% block body %}
    <h1>SectorArea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sector_area.id }}</td>
            </tr>
            <tr>
                <th>SectorTitle</th>
                <td>{{ sector_area.sectorTitle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sector_area_index') }}">back to list</a>

    <a href="{{ path('sector_area_edit', {'id': sector_area.id}) }}">edit</a>

    {{ include('sector_area/_delete_form.html.twig') }}
{% endblock %}